<h2 class="text-center" style="margin: 25px 0px 25px 0px;"><%= "Category: " + @category.name %></h2>

<div align="center">
<% if logged_in? and current_user.admin? %>
<span class="badge"><%= link_to "Edit Category Name", edit_category_path(@category) %> </span>
<% end %>
<%= will_paginate @category_articles %>
</div>

<%= render 'article', obj: @category_articles %>

<div align="center">
<%= will_paginate @category_articles %>
</div>